apiVersion: v1
kind: Service
metadata:
  name: memory-service
  namespace: default
  labels:
    app: memory-service
spec:
  type: ClusterIP
  selector:
    app: memory-service
  ports:
  - port: 8081
    targetPort: 8081
    protocol: TCP
    name: http
