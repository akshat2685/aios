apiVersion: v1
kind: Service
metadata:
  name: vector-db
  namespace: default
  labels:
    app: vector-db
spec:
  type: ClusterIP
  selector:
    app: vector-db
  ports:
  - port: 6333
    targetPort: 6333
    protocol: TCP
    name: http
