apiVersion: v1
kind: Service
metadata:
  name: agent-pods
  namespace: default
  labels:
    app: agent-pods
spec:
  type: ClusterIP
  selector:
    app: agent-pods
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
