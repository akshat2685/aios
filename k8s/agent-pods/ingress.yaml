apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: agent-pods-ingress
  namespace: default
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - host: agent-pods.aios.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: agent-pods
            port:
              number: 8080
