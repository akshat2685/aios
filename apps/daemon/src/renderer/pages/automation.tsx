import { WorkflowEngine } from '@/components/workflow';

export default function AutomationPage() {
  return (
    <div className="h-full overflow-hidden flex flex-col">
      <WorkflowEngine />
    </div>
  );
}
