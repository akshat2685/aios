// Injects random failures (delays, errors, malformed responses) into providers
export class FailureInjector {
  static injectLatency(minMs: number, maxMs: number) {
    // Stub
  }
  
  static injectError(errorRate: number) {
    // Stub
  }
}
