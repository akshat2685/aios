{
  "name": "@aios/context",
  "version": "0.1.0",
  "private": true,
  "description": "Context Engine for AIOS",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest run",
    "test:watch": "vitest",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@aios/core": "*",
    "@aios/config": "*",
    "@aios/types": "*",
    "@aios/knowledge": "*"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0",
    "vitest": "^1.0.0",
    "rimraf": "^5.0.0"
  }
}
