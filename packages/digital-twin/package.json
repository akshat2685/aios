{
  "name": "@aios/digital-twin",
  "version": "0.1.0",
  "private": true,
  "description": "Digital Twin — Persistent User Model for AIOS",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@aios/core": "*",
    "@aios/types": "*",
    "@aios/storage": "*",
    "@aios/memory": "*"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0",
    "rimraf": "^5.0.0"
  }
}
